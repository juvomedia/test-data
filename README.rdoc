== TestData

Test fixtures for LoveIt apps that require remote resources, like importing
images from a URL or a bookmarklet.

== Deploying

ey deploy --account='loveit' --app='testdata' --environment='prod_testdata' --ref='master' --no-migrate
